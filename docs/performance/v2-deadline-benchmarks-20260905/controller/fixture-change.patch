--- a/internal/sessionv2/hotpath_probe_test.go
+++ b/internal/sessionv2/hotpath_probe_test.go
@@ -19,3 +19,3 @@
 			for id := 1; id <= count; id++ {
-				c.groups[uint64(id)] = &pendingGroup{admitted: now.Add(time.Duration(id) * time.Nanosecond)}
+				c.insertGroup(uint64(id), &pendingGroup{admitted: now.Add(time.Duration(id) * time.Nanosecond)})
 			}
